<?php

if ( ! defined( 'ABSPATH' ) ) {
	exit; // Exit if accessed directly
}

/**
 * Generated by the WordPress Meta Box Generator at http://goo.gl/8nwllb
 */
class Caixabank_Meta_Box {
	private $screens = array(
		'caixabank_orders',
	);
	private $fields = array(
		array(
			'id' => '_caixabank_description',
			'label' => 'Description',
			'type' => 'textarea',
		),
		array(
			'id' => '_caixabank_user',
			'label' => 'User',
			'type' => 'text',
		),
		array(
			'id' => '_caixabank_price',
			'label' => 'Price',
			'type' => 'number',
		),
		array(
			'id' => '_caixabank_use_tax',
			'label' => 'Use TAX',
			'type' => 'checkbox',
		),
		array(
			'id' => '_caixabank_use_irpf',
			'label' => 'Use IRPF',
			'type' => 'checkbox',
		),
		array(
			'id' => '_caixabank_tax',
			'label' => 'TAX',
			'type' => 'number',
		),
		array(
			'id' => '_caixabank_irpf',
			'label' => 'IRPF',
			'type' => 'number',
		),
		array(
			'id' => '_caixabank_date',
			'label' => 'Date',
			'type' => 'date',
		),
		array(
			'id' => '_caixabank_time',
			'label' => 'Time',
			'type' => 'time',
		),
	);

	/**
	 * Class construct method. Adds actions to their respective WordPress hooks.
	 */
	public function __construct() {
		add_action( 'add_meta_boxes', array( $this, 'add_meta_boxes' ) );
		add_action( 'save_post', array( $this, 'save_post' ) );
	}

	/**
	 * Hooks into WordPress' add_meta_boxes function.
	 * Goes through screens (post types) and adds the meta box.
	 */
	public function add_meta_boxes() {
		foreach ( $this->screens as $screen ) {
			add_meta_box(
				'caixabank-order-metabox',
				__( 'caixabank_order_metabox', 'caixabank-tools-official' ),
				array( $this, 'add_meta_box_callback' ),
				$screen,
				'advanced',
				'default'
			);
		}
	}

	/**
	 * Generates the HTML for the meta box
	 *
	 * @param object $post WordPress post object
	 */
	public function add_meta_box_callback( $post ) {
		wp_nonce_field( 'caixabank_order_metabox_data', 'caixabank_order_metabox_nonce' );
		$this->generate_fields( $post );
	}

	/**
	 * Generates the field's HTML for the meta box.
	 */
	public function generate_fields( $post ) {
		$output = '';
		foreach ( $this->fields as $field ) {
			$label = '<label for="' . $field['id'] . '">' . $field['label'] . '</label>';
			$db_value = get_post_meta( $post->ID, 'caixabank_order_metabox_' . $field['id'], true );
			switch ( $field['type'] ) {
				case 'checkbox':
					$input = sprintf(
						'<input %s id="%s" name="%s" type="checkbox" value="1">',
						$db_value === '1' ? 'checked' : '',
						$field['id'],
						$field['id']
					);
					break;
				case 'textarea':
					$input = sprintf(
						'<textarea class="large-text" id="%s" name="%s" rows="5">%s</textarea>',
						$field['id'],
						$field['id'],
						$db_value
					);
					break;
				default:
					$input = sprintf(
						'<input %s id="%s" name="%s" type="%s" value="%s">',
						$field['type'] !== 'color' ? 'class="regular-text"' : '',
						$field['id'],
						$field['id'],
						$field['type'],
						$db_value
					);
			}
			$output .= $this->row_format( $label, $input );
		}
		echo '<table class="form-table"><tbody>' . $output . '</tbody></table>';
	}

	/**
	 * Generates the HTML for table rows.
	 */
	public function row_format( $label, $input ) {
		return sprintf(
			'<tr><th scope="row">%s</th><td>%s</td></tr>',
			$label,
			$input
		);
	}
	/**
	 * Hooks into WordPress' save_post function
	 */
	public function save_post( $post_id ) {
		if ( ! isset( $_POST['caixabank_order_metabox_nonce'] ) )
			return $post_id;

		$nonce = $_POST['caixabank_order_metabox_nonce'];
		if ( !wp_verify_nonce( $nonce, 'caixabank_order_metabox_data' ) )
			return $post_id;

		if ( defined( 'DOING_AUTOSAVE' ) && DOING_AUTOSAVE )
			return $post_id;

		foreach ( $this->fields as $field ) {
			if ( isset( $_POST[ $field['id'] ] ) ) {
				switch ( $field['type'] ) {
					case 'email':
						$_POST[ $field['id'] ] = sanitize_email( $_POST[ $field['id'] ] );
						break;
					case 'text':
						$_POST[ $field['id'] ] = sanitize_text_field( $_POST[ $field['id'] ] );
						break;
				}
				update_post_meta( $post_id, 'caixabank_order_metabox_' . $field['id'], $_POST[ $field['id'] ] );
			} else if ( $field['type'] === 'checkbox' ) {
				update_post_meta( $post_id, 'caixabank_order_metabox_' . $field['id'], '0' );
			}
		}
	}
}
new Caixabank_Meta_Box;